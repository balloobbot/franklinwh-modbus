# Network Scanner Requirements
# Install with: pip install -r network_scanner_requirements.txt

# Required for Modbus support
pymodbus>=3.0.0

# Required for HTTP-based device detection
requests>=2.25.0

# Required for mDNS/Bonjour discovery (cross-platform)
# Works on Linux, macOS, and Windows
zeroconf>=0.40.0

# Optional - for enhanced table output
tabulate>=0.8.9

# Optional - for colored terminal output
colorama>=0.4.4
