--- /tmp/claude-1000/-home-paulus-dev-hass-franklinwh-modbus/f780bbe6-f2a9-4ef0-953c-93c1d615b287/scratchpad/mc-base/src/modbus_connection/model/component.py	2026-08-09 17:26:38.923948432 -0400
+++ /tmp/claude-1000/-home-paulus-dev-hass-franklinwh-modbus/f780bbe6-f2a9-4ef0-953c-93c1d615b287/scratchpad/mc/src/modbus_connection/model/component.py	2026-08-09 17:28:24.435832096 -0400
@@ -15,8 +15,15 @@
 from .fields import CoilField, DiscreteInputField, RegisterField, _BitField
 
 if TYPE_CHECKING:
+    from collections.abc import Callable
+
     from .._protocol import ModbusUnit
 
+    #: Resolves a group's stride or offset once the owning layout has been read.
+    type Placement = Callable[[Component], int]
+else:
+    Placement = object
+
 
 def _partition[F](
     fields: dict[str, F], keep: set[str]
@@ -36,9 +43,11 @@
     _bit_fields: dict[str, _BitField] = {}
 
     declared_fields: Mapping[str, RegisterField[Any] | _BitField] = MappingProxyType({})
-    # repeating_group fields, split by count kind: a fixed ``int`` count is static
-    # (its instances fold into the normal read like ordinary fields), a
-    # ``RegisterField`` count is read at poll time (the two-phase repeating path).
+    # repeating_group fields, split by when the layout can be placed. A group is
+    # static — its instances fold into the normal read like ordinary fields —
+    # only when its count is a fixed ``int`` *and* its placement is fixed too. A
+    # count read from a register, or a stride/offset resolved from one, defers
+    # the group to the two-phase repeating path.
     _static_groups: dict[str, RepeatingGroupField[Any]] = {}
     _repeating_fields: dict[str, RepeatingGroupField[Any]] = {}
 
@@ -71,6 +80,14 @@
     # effect on a non-repeating component, whose instance offset is 0.
     scale_in_block: bool = False
 
+    # The same knob for a ``repeating_group``'s count register. By default a
+    # count is a point of the layout that owns the block rather than of the
+    # block, so it does not shift with the instance — which is what every
+    # SunSpec model means, and the safer reading of a map in general. Set this
+    # on a sub-unit whose repeated block genuinely carries its own count
+    # register, so instance *i* reads the count at ``count + i * stride``.
+    count_in_block: bool = False
+
     def __init_subclass__(cls, **kwargs: Any) -> None:
         super().__init_subclass__(**kwargs)
         registers: dict[str, RegisterField[Any]] = {}
@@ -87,9 +104,7 @@
                     bits[name] = value
                     declared[name] = value
                 elif isinstance(value, RepeatingGroupField):
-                    target = (
-                        static_groups if isinstance(value.count, int) else repeating
-                    )
+                    target = static_groups if value.is_static else repeating
                     target[name] = value
         cls._register_fields = registers
         cls._bit_fields = bits
@@ -104,10 +119,16 @@
         *,
         base_offset: int = 0,
         _instance_offset: int = 0,
+        _root: Component | None = None,
     ) -> None:
         self._unit = unit
         self._index = index
         self._base_offset = base_offset
+        # The component whose block these addresses are relative to: this one
+        # unless it is a repeated sub-unit, in which case the outermost owner.
+        # A dynamic stride or offset is resolved against it, because the count
+        # points that size a nested block are points of the outermost layout.
+        self._root = _root if _root is not None else self
         # Internal, set by repeating_group when building instances: the
         # per-instance shift within the parent's block, applied to fields and
         # bits but not to scale registers (those follow the block itself),
@@ -335,11 +356,36 @@
         count: RegisterField[int] | RegisterField[float] | int,
         component_class: type[C],
         *,
-        stride: int,
+        stride: int | Placement,
+        offset: int | Placement = 0,
     ) -> None:
         self.count = count
         self.component_class = component_class
         self.stride = stride
+        self.offset = offset
+
+    @property
+    def is_static(self) -> bool:
+        """Whether this group can be placed before the device is read."""
+        return (
+            isinstance(self.count, int)
+            and not callable(self.stride)
+            and not callable(self.offset)
+        )
+
+    def placement(self, root: Component) -> tuple[int, int]:
+        """Resolve ``(stride, offset)`` against the layout that owns the block.
+
+        Raises ``ValueError`` if a resolved stride is not positive.
+        """
+        stride = self.stride(root) if callable(self.stride) else self.stride
+        offset = self.offset(root) if callable(self.offset) else self.offset
+        if stride <= 0:
+            raise ValueError(
+                f"repeating_group {self.name!r} resolved to stride {stride};"
+                " a dynamic stride must be > 0 once the device has been read"
+            )
+        return stride, offset
 
     def __set_name__(self, owner: type, name: str) -> None:
         self.name = name
@@ -362,7 +408,8 @@
     count: RegisterField[int] | RegisterField[float] | int,
     component_class: type[C],
     *,
-    stride: int,
+    stride: int | Placement,
+    offset: int | Placement = 0,
 ) -> RepeatingGroupField[C]:
     """Create a repeated subcomponent field.
 
@@ -370,6 +417,16 @@
     accepted: the decoded count is truncated to an ``int``, and an
     unimplemented (``None``) count yields no instances.
 
+    ``stride`` and ``offset`` place the instances. Either may be a callable
+    taking the component whose block these addresses are relative to — the
+    outermost layout, not the immediate parent — and returning the resolved
+    value. Use one when the block's size is only known once the device has been
+    read: a SunSpec curve block is ``11 + NPt * 2`` registers wide, and ``NPt``
+    is a point of the model. A group placed this way is sized in the same
+    second pass that resolves register-read counts, so the values it depends on
+    have already been read; it is not folded into the parent's own block read
+    even when its count is a fixed ``int``.
+
     On readable ranges: a fixed-count group's instances are read from the
     parent's own plan, so their maps merge into it and must not describe the
     same addresses differently. Either let the parent's ``register_ranges``
@@ -379,10 +436,11 @@
     whose own map has holes the parent cannot express. Declaring the same
     addresses in both raises ``ValueError``.
 
-    Raises ``ValueError`` for a non-positive stride or negative fixed count.
+    Raises ``ValueError`` for a non-positive stride or negative fixed count. A
+    callable stride is checked when it resolves instead.
     """
-    if stride <= 0:
+    if not callable(stride) and stride <= 0:
         raise ValueError(f"repeating_group stride must be > 0, got {stride}")
     if isinstance(count, int) and count < 0:
         raise ValueError(f"a fixed count must be >= 0, got {count}")
-    return RepeatingGroupField(count, component_class, stride=stride)
+    return RepeatingGroupField(count, component_class, stride=stride, offset=offset)
--- /tmp/claude-1000/-home-paulus-dev-hass-franklinwh-modbus/f780bbe6-f2a9-4ef0-953c-93c1d615b287/scratchpad/mc-base/src/modbus_connection/model/_component_base.py	2026-08-09 17:26:38.923863145 -0400
+++ /tmp/claude-1000/-home-paulus-dev-hass-franklinwh-modbus/f780bbe6-f2a9-4ef0-953c-93c1d615b287/scratchpad/mc/src/modbus_connection/model/_component_base.py	2026-08-09 17:28:24.435921000 -0400
@@ -7,6 +7,7 @@
 from typing import TYPE_CHECKING, Any, cast
 
 from ._const import Raw, RegisterSpace
+from .fields import RegisterField as RegisterFieldType
 from ._planning import ReadItem, _merge_raw, _Readable
 from ._ranges import DeviceRanges
 from .component_group import ComponentGroup
@@ -23,6 +24,8 @@
 
     _base_offset: int = 0
     _instance_offset: int = 0
+    _root: Component
+    count_in_block: bool = False
     _static_groups: dict[str, RepeatingGroupField[Any]] = {}
     _repeating_fields: dict[str, RepeatingGroupField[Any]] = {}
 
@@ -75,11 +78,13 @@
         # fields only, so shared scale factors stay in the parent's fixed block —
         # unless the sub-unit sets ``scale_in_block``, which moves each instance's
         # scale registers with its shift too (a block carrying its own factors)
+        stride, offset = field.placement(self._root)
         return [
             field.component_class(
                 self._unit,
                 base_offset=self._base_offset,
-                _instance_offset=self._instance_offset + i * field.stride,
+                _instance_offset=self._instance_offset + offset + i * stride,
+                _root=self._root,
             )
             for i in range(start, stop)
         ]
@@ -91,14 +96,18 @@
         for name, field in self._repeating_fields.items():
             # a register-count group's count is a RegisterField (see the split above)
             count_field = cast("RegisterField[Any]", field.count)
+            if not isinstance(count_field, RegisterFieldType):
+                continue  # a fixed count deferred only for its dynamic placement
             count_field.name = name  # the decoded count lands in ``_counts[name]``
+            # A count register is a point of the layout that owns the block
+            # rather than of the block, so by default it does not move with the
+            # instance — the same default, and the same reasoning, as
+            # ``scale_register``. ``count_in_block`` opts back in.
+            address = count_field.address + self._base_offset
+            if self.count_in_block:
+                address += self._instance_offset
             items.append(
-                ReadItem(
-                    count_field.address + self._base_offset + self._instance_offset,
-                    count_field,
-                    self._counts,
-                    self._count_space,
-                )
+                ReadItem(address, count_field, self._counts, self._count_space)
             )
         return items
 
@@ -172,8 +181,11 @@
         # trimming its instances and dropping the cached pooled group on a change.
         instances: list[Component] = []
         for name, field in self._repeating_fields.items():
-            value = self._counts.get(name)
-            count = max(0, int(value)) if value is not None else 0
+            if isinstance(field.count, int):
+                count = field.count  # deferred only because its placement is
+            else:
+                value = self._counts.get(name)
+                count = max(0, int(value)) if value is not None else 0
             existing = self._groups.get(name, [])
             if len(existing) != count:
                 existing = existing[:count] + self._build_instances(
--- /tmp/claude-1000/-home-paulus-dev-hass-franklinwh-modbus/f780bbe6-f2a9-4ef0-953c-93c1d615b287/scratchpad/mc-base/src/modbus_connection/model/manual.py	2026-08-09 17:26:38.923889808 -0400
+++ /tmp/claude-1000/-home-paulus-dev-hass-franklinwh-modbus/f780bbe6-f2a9-4ef0-953c-93c1d615b287/scratchpad/mc/src/modbus_connection/model/manual.py	2026-08-09 17:28:24.434816550 -0400
@@ -31,6 +31,7 @@
         discrete_ranges: tuple[Range, ...] | None = None,
     ) -> None:
         self._unit = unit
+        self._root = self  # addresses here are absolute; nothing nests above it
         self._max_gap = max_gap
         self._max_span = max_span
         # Readable address ranges per table; a table left None falls back to
@@ -70,7 +71,7 @@
         if isinstance(target, RepeatingGroupField):
             if space is not None:
                 raise ValueError("space does not apply to a repeating_group")
-            if isinstance(target.count, int):
+            if target.is_static:
                 self._static_groups[key] = target
                 self._groups[key] = self._build_instances(target, 0, target.count)
             else:
